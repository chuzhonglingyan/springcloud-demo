server:
  servlet:
    context-path: /
  port: 7003
spring:
  application:
    name: order-service
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:5060/eureka

